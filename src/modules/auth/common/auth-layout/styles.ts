import { SxProps } from "@mui/material";

export const authLayoutSx: Record<string, SxProps> = {
  image: {
    backgroundImage: 'url("/src/assets/img/auth.jpg")',
    backgroundPosition: "center",
    backgroundSize: "cover",
    borderRadius: 4,
  },
};
