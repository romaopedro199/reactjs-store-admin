import { SxProps } from "@mui/material";

export const inputSx: Record<string, SxProps> = {
  input: {
    ".MuiOutlinedInput-root": {
      borderRadius: 3,
    },
    label: { color: "text.primary" },
  },
};
